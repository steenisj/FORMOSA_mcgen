This depends on MCPTree in `../../decayMCP`.

In order to make condor submission easier, we make a link to that directory here and compile against the "local" copy.

Run `. setup.sh` to make that link, then just `make`.
